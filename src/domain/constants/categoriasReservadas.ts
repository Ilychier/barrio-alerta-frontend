/**
 * Categorías reservadas del sistema (regla de dominio compartida con el backend).
 * Estas categorías NO se ofrecen en el formulario manual — se crean solo por
 * flujos automáticos (botón SOS / fin de emergencia).
 */
export const CATEGORIA_SOS = 4;
export const CATEGORIA_FIN_EMERGENCIA = 5;
